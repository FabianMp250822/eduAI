rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allows read and write access to the entire database for anyone.
    // WARNING: This is insecure and should only be used for local development and testing.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
